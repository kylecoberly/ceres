<section class="metrics">
	<h2>Class Metrics</h2>
	<div class="hiring">
		{{#each model as |cohort|}}
			{{#if cohort.isActive}}
				{{cohort-job-stats cohort=cohort}}
			{{/if}}
		{{/each}}
	</div>
</section>
